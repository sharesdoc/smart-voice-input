# VoiceInput 运行时依赖（macOS）
# 安装：~/ins/miniconda/bin/python -m pip install -r requirements.txt

# 菜单栏 UI（基于 PyObjC）
rumps>=0.4.0
pyobjc-core>=10.0
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-Quartz>=10.0

# 录音
sounddevice>=0.4.6
numpy>=1.24

# 本地语音识别（Whisper / CTranslate2，跨 arm64/x86_64）
faster-whisper>=1.0.0

# SenseVoice 极速本地引擎（非自回归，~70x 实时，自带标点）
funasr>=1.3.0
torch>=2.2
torchaudio>=2.2
soundfile>=0.12

# MLX Whisper（Apple Silicon MLX 加速，比 CPU faster-whisper 快很多；仅 arm64 Mac）
mlx-whisper>=0.4.0; platform_machine == "arm64" and sys_platform == "darwin"

# 全局热键监听（双击 Command）
pynput>=1.7.6

# LLM 调用（Ollama / OpenAI 兼容）
httpx>=0.27

# 阿里云 DashScope Fun-ASR 在线识别（轻量，纯 Python；用环境变量 DASHSCOPE_API_KEY）
dashscope>=1.20

# 敏感信息存储（macOS 钥匙串）
keyring>=24.0

# ---- 开发/测试（被 ./install -b 用于运行单元测试与覆盖率门禁）----
pytest>=8.0
pytest-cov>=5.0
