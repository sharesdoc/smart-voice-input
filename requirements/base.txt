# VoiceInput 基础依赖（始终安装）——与具体 ASR 引擎无关的通用部分
# UI / 录音 / 音频 I/O / 全局热键 / HTTP / 钥匙串 / 开发测试

# 菜单栏 UI（基于 PyObjC）
rumps>=0.4.0
pyobjc-core>=10.0
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-Quartz>=10.0

# 录音 + 音频 I/O（保存/读取 wav，通用）
sounddevice>=0.4.6
numpy>=1.24
soundfile>=0.12

# 全局热键监听（双击 Command）
pynput>=1.7.6

# LLM 调用（Ollama / OpenAI 兼容）+ OpenAI 兼容在线 ASR 也走 httpx
httpx>=0.27

# 敏感信息存储（macOS 钥匙串）
keyring>=24.0

# ---- 开发/测试（被 ./install -b 用于运行单元测试与覆盖率门禁）----
pytest>=8.0
pytest-cov>=5.0
