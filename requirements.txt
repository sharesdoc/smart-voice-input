# VoiceInput 全量依赖入口（macOS）= 基础 + 全部 ASR 引擎
#
# 说明：`./install -i` 已改为「按需交互安装」，只安装你勾选的引擎依赖；
#       本文件仅用于「一键全装/参考全量清单」：
#           ~/ins/miniconda/bin/python -m pip install -r requirements.txt
#       单独安装某一引擎可直接： pip install -r requirements/asr-<engine>.txt
#
# 分组明细见 requirements/ 目录：
#   base.txt                 基础（UI/录音/热键/HTTP/钥匙串/测试，始终需要）
#   asr-faster-whisper.txt   本地 faster-whisper（不依赖 torch）
#   asr-sensevoice.txt       SenseVoice（funasr + torch，最大）
#   asr-mlx.txt              MLX Whisper（仅 Apple Silicon）
#   asr-online.txt           阿里云 DashScope 在线（最轻量）
#   （OpenAI 兼容在线 ASR 无需额外依赖，复用 base 的 httpx）

-r requirements/base.txt
-r requirements/asr-faster-whisper.txt
-r requirements/asr-sensevoice.txt
-r requirements/asr-mlx.txt
-r requirements/asr-online.txt
